# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets & Config
.env
*.json
# D 드라이브 키 경로는 코드에 있지만, 만약 복사해올 경우를 대비
google_vertex_key.json

# Research Artifacts
court_logs/
oracle_views/

# Project Generated Files
chroma_db/
vlm_courtroom/outputs/
vlm_courtroom/inputs/
vlm_courtroom/chroma_db/
*.png

__pycache__/
*.pyc
.env
*.json
.DS_Store
*.html
*.pdf
results/
outputs/
*.npy

# —— VLM2Torque: 자동 생성되는 파생 파일 (재현 가능, 버전관리 불필요) ——
# draw_path_markers.py가 매번 새로 만드는 마커 삽입 XML/YAML
dial_mpc/dial_mpc/models/unitree_go2/*_viz.xml
dial_mpc/dial_mpc/examples/*_viz.yaml

# 야간 파이프라인 실행 로그
logs/
nohup.out

# Paper (drafted locally, not tracked in this branch)
paper/
data/

# Neural A* (별도 repo, 자체 .git+submodule 있음 -- vlm_courtroom/dial_mpc와
# 나란히 두되 우리 repo git 히스토리에는 안 들어가게)
neural-astar/neural-astar/
